# Markdown-slides

Basic bash + template scaffold for creating reveal.js markdown slides.

## Quick start

```bash
chmod +x ./create_markdown_slides.sh
./create_markdown_slides.sh ./my-talk "My Talk Title"
```

This generates a reveal.js-ready markdown slide deck in the target directory.
